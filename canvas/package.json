{
  "name": "plasma-demo",
  "version": "1.0.0",
  "description": "Modern Zig WebAssembly Plasma Software Renderer Demo",
  "main": "web/main.js",
  "type": "module",
  "scripts": {
    "build": "echo '🚀 Compiling Zig to WebAssembly (ReleaseSmall)...' && zig build -Doptimize=ReleaseSmall",
    "build:debug": "echo '🚀 Compiling Zig to WebAssembly (Debug)...' && zig build",
    "build:dist": "bun run build && echo '📦 Initiating full asset compression pipeline...' && bun run scripts/build_dist.ts",
    "dev": "bun run build && echo '🌐 Starting local dev server...' && bun x serve web -l 9000",
    "start": "bun x serve web -l 9000"
  },
  "keywords": [
    "zig",
    "wasm",
    "plasma",
    "retro"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/clean-css": "^4.2.11",
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0"
  }
}